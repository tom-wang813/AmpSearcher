# AmpSearcher 環境變量示例

# 本文件用於定義環境變量。請將此文件複製為 `.env` 並填寫您的實際值。
# `.env` 文件不應提交到版本控制中，以保護敏感信息。

# --- MLflow 配置 ---
# 如果您使用 MLflow 進行實驗追蹤，可以在此處配置 MLflow 服務器的 URI。
# 默認值通常是本地文件系統的 `./mlruns` 目錄。
# MLFLOW_TRACKING_URI=http://localhost:5000

# --- API 配置 ---
# 配置 API 服務監聽的主機和端口。
# API_HOST=0.0.0.0
# API_PORT=8000

# --- 敏感信息/外部服務 API 密鑰 (示例) ---
# 如果您的項目需要與外部付費服務或需要身份驗證的 API 交互，
# 請在此處定義您的 API 密鑰。例如：
# SOME_EXTERNAL_SERVICE_API_KEY=請在此處填寫您的外部服務 API 密鑰
# 另一個例子：
# OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx # 假設您集成了 OpenAI API

# 請注意：任何敏感信息都應通過環境變量管理，而不是直接寫入代碼或配置文件。